services:
  db:
    image: postgres:15
    container_name: jpop_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: jpop-idols
    ports:
      - "5433:5432"

volumes:
  pgdata:
